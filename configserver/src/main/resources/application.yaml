server:
  port: 9988
spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name:config-server
  profiles:
    active: git
  cloud:
    config:
      server:
        #native:
        #  search-locations: classpath:/config
        git:
          uri: https://github.com/jmmunozr/test-config
          searchPaths: "{application}/, env/, domain/"
          default-label: master
#spring.cloud.config.server.git.search-paths=test-config
